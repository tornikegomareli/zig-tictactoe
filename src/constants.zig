pub const SCREEN_WIDTH = 800;
pub const SCREEN_HEIGHT = 450;
pub const GRID_SIZE = 300;
pub const CELL_SIZE: i32 = GRID_SIZE / 3;
pub const GRID_X: i32 = (SCREEN_WIDTH - GRID_SIZE) / 2;
pub const GRID_Y: i32 = (SCREEN_HEIGHT - GRID_SIZE) / 2;
